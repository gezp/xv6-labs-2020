(1) a0保存第一个参数，以此类推，13保存在a2里。
(2) 46行call f和g,编译器已经优化，f(8)+1优化为12，f(x)优化成g(x)一样的代码。
(3) printf函数位于0x630。
(4) 输出“HE110 World”，如果是大端模式，i = 0x726c6400，57616 不需要改变。（因为i是按照字符串模式，一个一个字节打印，57616是整体打印）
(5) 会输出一个不确定的数，取决于寄存器a2的值。